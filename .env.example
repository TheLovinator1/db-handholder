# Environment variables for db-handholder
# Copy this file to .env and adjust values as needed.
# Note: DEBUG reads string values; use True or False (capitalized, no quotes).

# Django secret key (change for production)
# You can generate a new key using Django's get_random_secret_key() function:
# uv run python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=

# Enable Django debug mode (True for local development, False for production)
DJANGO_DEBUG=False

# Comma-separated list of allowed hosts when DEBUG=False
# Example: example.com,www.example.com,dbh.example.com
DJANGO_ALLOWED_HOSTS=
